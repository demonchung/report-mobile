/**
 * 处理明细表
 * @param options
 */
 function handleListData(options: H3.Chart.ChartOptions): H3.Chart.ChartData {
  // 待定,后台返回数据如果能做到和透视表交叉表一样的数据返回,三个表格可以用同一套逻辑处理
  return {
    
  };
}

export default handleListData;
